# Not a real scheduler

It's a home assignment project


## Setup 

Install pipenv:

```bash
pip install pipenv
```

Install the dependencies:

```bash
pipenv install
```

## Running it

Activate virtual env:

```bash
pipenv shell
```

Run

```bash
python main.py
```

Running tests:

```bash
pytest
```